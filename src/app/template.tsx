import { PageTransitionWidget } from "@/components/general";

export default function Template({ children }: { children: React.ReactNode }) {
  return <PageTransitionWidget>{children}</PageTransitionWidget>;
}
